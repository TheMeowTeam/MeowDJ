extends layout.jade

block preInit
  script(src='/js/dependencies/sails.io.js')

block body
  //div.centered.align-center
  //  img.center(src='/images/logo.png')
  //  if (data.user)
  //    h2.primary-color Hello #{data.user.username}
  //    h3.primary-color What would you like to do? :)
  //    if (!data.userRoom)
  //      a.btn.btn-blue(href='/create') Create my room!
  //    else
  //      a.btn.btn-blue(href = "/" + data.userRoom) Join my room!
  //  else
  //    h1.primary-color Welcome to MeowDJ
  //    h2.primary-color Meow.dj is a room-based music sharing provider in Node.js
  //    a.btn.btn-blue#login(href='#') Join The Party!

  div.header
    div.container
      img.logo(src='/images/logo-horizontal.png')

      div.join
        if (data.user)
          if (data.userRoom)
            button.button.red#room Join my room!
          else
            button.button.red#create Create my room!
        else
          button.button.red#login Join the party!

  div.content
    div.container
      div.quote
        p.character “
        p The only real comment from a piece of music is another piece of music.
        p.character.last ”
        p.author Igor Stravinski
